sudo apt install mariadb-server #mysql server
sudo mysql_secure_installation
sudo mysql-u root -p
CREATE DATABASE exampledb4;
CREATE TABLE adatok (datum  DATETIME,pitch INT ,roll INT,yaw INT,backwardforward INT,rightleft INT,updown INT);